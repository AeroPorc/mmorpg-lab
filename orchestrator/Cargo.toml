[package]
name = "orchestrator"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
redis = "0.24"
uuid = { version = "1.0", features = ["v4"] }
serde_json = "1.0"
shared = { path = "../shared" }
